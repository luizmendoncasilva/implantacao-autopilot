# Usuários

Pasta reservada para os protótipos da trilha Usuários. Ainda não existe item correspondente no menu lateral do AutoPilot — crie a trilha aqui quando ela entrar em discovery, seguindo o mesmo padrão das demais pastas em `prototype/` (index.html + css/js próprios, reaproveitando `prototype/shared/`).
